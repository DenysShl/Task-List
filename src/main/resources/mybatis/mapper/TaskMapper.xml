<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.tasklist.repository.TaskRepository">

    <resultMap id="TaskResultMap" type="org.example.tasklist.model.Task" autoMapping="false">
        <id property="id" column="task_id"/>
        <result property="title" column="task_title"/>
        <result property="description" column="task_description"/>
        <result property="status" column="task_status"/>
        <result property="expirationDate" column="task_expiration_date"/>
    </resultMap>

    <select id="findById" resultMap="TaskResultMap">
        SELECT
            t.id as task_id,
            t.title as task_title,
            t.description as task_description,
            t.status as task_status,
            t.expiration_date as task_expiration_date
        FROM task t
        WHERE t.id = #{id}
    </select>

    <select id="findAllByUserId" resultMap="TaskResultMap">
        SELECT
            t.id as task_id,
            t.title as task_title,
            t.description as task_description,
            t.status as task_status,
            t.expiration_date as task_expiration_date
        FROM task t
                 JOIN users_tasks ut ON t.id = ut.task_id
        WHERE ut.user_id = #{userId}
    </select>

    <insert id="create" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO task (title, description, status, expiration_date)
        VALUES (#{title}, #{description}, #{status}, #{expirationDate})
    </insert>

    <insert id="assignToUserById">
        INSERT INTO users_tasks ( task_id, user_id) VALUES (#{taskId}, #{userId})
    </insert>

    <update id="update">
        UPDATE task
        SET title = #{title},
            description = #{description},
            status = #{status},
            expiration_date = #{expirationDate}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM task
        WHERE id = #{id}
    </delete>

</mapper>